<?php
// Common functions or configurations
// Add any common functions or configurations here
function sanitize_input($data) {
    $data = trim($data);
    $data = stripslashes($data);
    $data = htmlspecialchars($data);
    return $data;
}
?>
